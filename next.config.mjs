/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['plus.unsplash.com', 'images.unsplash.com' ,  "readymadeui.com" , "images.pexels.com"],
    },
  };
  
  export default nextConfig;
  